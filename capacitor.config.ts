import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'material.dashboard.2.react',
  appName: 'material-dashboard-2-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
